
spring:
  application:
    name: DEPARTMENT-SERVICE
  config:
    import: configserver:https://api-cloud-config.herokuapp.com
  cloud:
    config:
      enabled: true
      name: department
      profile: prod
      label: master
server:
  port: ${PORT:5002}

